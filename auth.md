# `oc auth`

> 認可の状態を調べる

[`oc`](oc.md) / `auth`

## Usage

```
oc auth [flags] [options]
```

## Subcommands

- [`can-i`](auth/can-i.md) — ある操作が許可されるかどうかを確認する
- [`reconcile`](auth/reconcile.md) — RBAC のロール、ロールバインディング、クラスタロール、クラスタロールバインディングの各オブジェクトのルールを reconcile します
- [`whoami`](auth/whoami.md) — 実験的機能: 自分自身のサブジェクト属性を確認する

> 各コマンドの詳細については "oc auth `<command>` --help" を使用してください。

> すべてのコマンドに共通するグローバルオプションの一覧は "oc options" で確認できます。

---

<sub>`$ oc auth --help` / `gen-oc-help.py` で生成</sub>
