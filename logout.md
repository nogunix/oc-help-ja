# `oc logout`

> 現在のサーバーセッションを終了する

[`oc`](oc.md) / `logout`

## Usage

```
oc logout [flags] [options]
```

保存されたトークンを消去して、アクティブなセッションからログアウトします。

ログイン後、認証トークンは設定ファイルに保存されます。このコマンドは、サーバー側でそのトークンを削除し、設定ファイルからも削除します。

Kerberos やクライアント証明書などの別の認証方式を使用している場合、チケットやクライアント証明書は通常他のプログラムが管理しているため、現在のシステムからは削除されません。その場合は設定ファイルを削除することで、証明書のローカルコピーやサーバーへのログイン記録を消去できます。

ログアウト後にサーバーへログインし直すには 'oc login' を使用します。

## Examples

```bash
# ログアウトする
oc logout
```

> すべてのコマンドに共通するグローバルオプションの一覧は "oc options" で確認できます。

---

<sub>`$ oc logout --help` / `gen-oc-help.py` で生成</sub>
