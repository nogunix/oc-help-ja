# `oc registry`

> レジストリを操作するためのコマンド

[`oc`](oc.md) / `registry`

## Usage

```
oc registry COMMAND [flags] [options]
```

OpenShift の統合レジストリを管理する

これらのコマンドは、OpenShift の統合レジストリの操作を支援します。

## Subcommands

- [`login`](registry/login.md) — 統合レジストリにログインする

> 各コマンドの詳細については "oc registry `<command>` --help" を使用してください。

> すべてのコマンドに共通するグローバルオプションの一覧は "oc options" で確認できます。

---

<sub>`$ oc registry --help` / `gen-oc-help.py` で生成</sub>
