# `oc adm ocp-certificates monitor-certificates`

> Watch platform certificates

[`oc`](../../oc.md) / [`oc adm`](../../adm.md) / [`oc adm ocp-certificates`](../ocp-certificates.md) / `monitor-certificates`

## Usage

```
oc adm ocp-certificates monitor-certificates [options]
```

Watch the platform certificates in the cluster.

Experimental: This command is under active development and may change without notice.

## Examples

```bash
# Watch platform certificates
oc adm ocp-certificates monitor-certificates
```

> Use "oc options" for a list of global command-line options (applies to all commands).

---

<sub>`$ oc adm ocp-certificates monitor-certificates --help` / `gen-oc-help.py` で生成</sub>
