# `oc adm migrate`

> Migrate data in the cluster

[`oc`](../oc.md) / [`oc adm`](../adm.md) / `migrate`

## Usage

```
oc adm migrate [flags] [options]
```

Migrate resources on the cluster

These commands assist administrators in performing preventative maintenance on a cluster.

## Subcommands

- [`icsp`](migrate/icsp.md) — Update imagecontentsourcepolicy file(s) to imagedigestmirrorset file(s)
- [`template-instances`](migrate/template-instances.md) — Update template instances to point to the latest group-version-kinds

> Use "oc adm migrate `<command>` --help" for more information about a given command.

> Use "oc options" for a list of global command-line options (applies to all commands).

---

<sub>`$ oc adm migrate --help` / `gen-oc-help.py` で生成</sub>
