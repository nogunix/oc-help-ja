# `oc config new-kubelet-bootstrap-kubeconfig`

> Generate, make the server trust, and display a new kubelet /etc/kubernetes/kubeconfig

[`oc`](../oc.md) / [`oc config`](../config.md) / `new-kubelet-bootstrap-kubeconfig`

## Usage

```
oc config new-kubelet-bootstrap-kubeconfig [options]
```

## Examples

```bash
# Generate a new kubelet bootstrap kubeconfig
oc config new-kubelet-bootstrap-kubeconfig
```

> Use "oc options" for a list of global command-line options (applies to all commands).

---

<sub>`$ oc config new-kubelet-bootstrap-kubeconfig --help` / `gen-oc-help.py` で生成</sub>
