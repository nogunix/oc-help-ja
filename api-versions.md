# `oc api-versions`

> サーバーがサポートしている API バージョンを "group/version" の形式で表示する

[`oc`](oc.md) / `api-versions`

## Usage

```
oc api-versions [options]
```

## Examples

```bash
# サポートされている API バージョンを表示する
oc api-versions
```

> すべてのコマンドに共通するグローバルオプションの一覧は "oc options" で確認できます。

---

<sub>`$ oc api-versions --help` / `gen-oc-help.py` で生成</sub>
