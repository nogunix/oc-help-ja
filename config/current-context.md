# `oc config current-context`

> current-context を表示する

[`oc`](../oc.md) / [`oc config`](../config.md) / `current-context`

## Usage

```
oc config current-context [flags] [options]
```

## Examples

```bash
# current-context を表示する
oc config current-context
```

> すべてのコマンドに共通するグローバルオプションの一覧は "oc options" で確認できます。

---

<sub>`$ oc config current-context --help` / `gen-oc-help.py` で生成</sub>
