# `oc project`

> 別のプロジェクトに切り替える

[`oc`](oc.md) / `project`

## Usage

```
oc project [NAME] [flags] [options]
```

別のプロジェクトに切り替え、それを設定上のデフォルトにします。

コマンドラインでプロジェクトを指定しなかった場合、現在アクティブなプロジェクトの情報を表示します。このコマンドでは異なるサーバー上のプロジェクトにも接続できるため、別のサーバーに同名のプロジェクトが存在する場合があります。そのプロジェクトに切り替えると、'myapp-2' のような一意の名前を持つローカルコンテキストが新しく作成されます。プロジェクト名とは別の名前でコンテキストを作成済みの場合は、そのコンテキスト名を指定することもできます。

高度な設定を行う場合や、設定ファイルの内容を管理する場合は 'config' コマンドを使用してください。

## Examples

```bash
# 'myapp' プロジェクトに切り替える
oc project myapp

# 現在使用中のプロジェクトを表示する
oc project
```

## Options

- `-q, --short=false`
  true の場合、プロジェクト名のみを表示します

> すべてのコマンドに共通するグローバルオプションの一覧は "oc options" で確認できます。

---

<sub>`$ oc project --help` / `gen-oc-help.py` で生成</sub>
